<table class='table table-hover'>
  <tr>
    <th>Questionaire Name</th>
    <th>ID</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <tr>
    <tbody>
    <% @reviews.each do |taxi| %>
      <tr>
        <td><%= review.name %></td>
        <td><%= review.id %></td>
        <td><%= %></td>
        <td><%= link_to 'Show', review_path(taxi.id) %></td>
        <td><%= link_to 'Edit', '#' %></td>
        <td><%= link_to 'Destroy', '#', method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
  </tr>
</table>

<%= link_to "New Questionaire", new_review_path %>
